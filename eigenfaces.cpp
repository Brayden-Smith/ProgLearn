#include "eigenfaces.h"
#include <iostream>

FaceSpace::FaceSpace(std::string& path) : faceDatabase(Matrix(1, 1)), eigenFaces(Matrix(1, 1)), numFaces(0), numEigenFaces(0), numFacesAdded(0), numFacesRemoved(0) {
    //todo
}

FaceSpace::FaceSpace(Matrix faceMatrix) {
    //todo
}



